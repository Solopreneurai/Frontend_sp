import { Box, styled } from "@mui/material"

const Wrapper = styled(Box)({
    padding: "10px 30px"
})
function History() {
  return (
    <Wrapper>
       
    </Wrapper>
  )
}

export default History
